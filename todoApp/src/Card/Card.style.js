import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderWidth: 1,
    borderRadius: 10,
    margin: 10,
    backgroundColor: "#B9CFCF",
  },
  textCard: {
    flex: 1,
    padding: 10,
    justifyContent: "center",
  },

  title: {
    fontSize: 22,
    fontWeight: "400",
  },
});

export default styles;
